scopes:
  source:
    manual:

pull_request_rules:
  - name: VM1_MANUAL_SCOPE_CROSS_BASE_IMPACT
    conditions:
      - base = main
      - title = VM1_MANUAL_SCOPE_CONTEXT_LOSS_RISKY
      - label = vm1-manual-scope-impact-trigger
      - scope = docs
      - -scope = critical
    actions:
      merge: {}
