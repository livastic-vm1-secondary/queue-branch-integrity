queue_rules:
  - name: default
    merge_method: merge-batch
    batch_size: 2
    batch_max_wait_time: 10 min
    allow_queue_branch_edit: false
    queue_conditions: []
    merge_conditions:
      - check-success = @github-actions/queue-hold

pull_request_rules:
  - name: queue controlled pull requests
    conditions:
      - base = main
    actions:
      queue:
        name: default

merge_protections:
  - name: VM1 false queue-merged dependency chain
    if:
      - title = VM1_FALSE_MERGED_DEPENDENCY_CHAIN
    success_conditions:
      - base = main
