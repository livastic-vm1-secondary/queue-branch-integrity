commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm1-secondary
      - author = livastic-vm1-secondary

merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: vm1-stack-gate2-author-boundary
    batch_size: 1
    max_checks_retries: 0
    merge_method: merge
    update_method: merge
    queue_conditions:
      - title ~= ^VM1_STACK_GATE2_(OWNER|ATTACKER)_260923$
    merge_conditions:
      - title = VM1_STACK_GATE2_OWNER_260923
