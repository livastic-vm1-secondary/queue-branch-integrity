pull_request_rules:
  - name: VM1 review-thread pagination merge probe
    conditions:
      - head = vm1-review-thread-pagination-probe-20260912
      - "#review-threads-unresolved = 0"
    actions:
      merge:
        method: squash
