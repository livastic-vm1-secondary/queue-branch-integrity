pull_request_rules:
  - name: VM1 review-thread pagination production probe
    conditions:
      - head = vm1-review-thread-pagination-probe-20260912
      - "#review-threads-unresolved = 0"
    actions:
      label:
        add:
          - vm1-review-thread-pagination-fired
